import { describe, it, expect } from 'vitest'

import { shallowMount } from '@vue/test-utils'
import {{name}} from './{{name}}.vue'

describe('Main', () => {
  it('renders properly', () => {
    const wrapper = shallowMount({{name}})

    expect(wrapper.text()).toContain('Hello form {{name}}')
  })
})
