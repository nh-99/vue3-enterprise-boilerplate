import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import {{ name }} from './{{ name }}.vue'

describe('{{ name }}', () => {
  it('renders properly', () => {
    const wrapper = mount({{ name }}, { props: { msg: 'Hello Vitest' } })
    expect(wrapper.text()).toContain('Hello Vitest')
  })
})
